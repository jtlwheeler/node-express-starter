plugins {
    id 'com.avast.gradle.docker-compose' version '0.7.1'
}

subprojects {
    version = '1.0'
}

apply plugin: 'docker-compose'

dockerCompose {
    useComposeFiles = ['docker-compose.yml']
}

composeUp.dependsOn(":server:dockerBuildImage")